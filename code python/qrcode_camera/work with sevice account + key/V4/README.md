# AIMS Exhibit
A video feed that captures an image when the user covers the Region Of Interest in the frame (ROI).  
The image then is uploaded to the cloud and is downloadable via a qr code which appears on the screen.
# Installation
```bash
chmod +x setup.sh && ./setup.sh
```
this installs all requirements and sets run.sh as a startup script to be run. also removes screen blanking.